import React from 'react';

const EmptyCart = () => {
	return (
		<div className="empty-cart">
			<span >There are no products in your cart. Let's change that!</span>
		</div>
	);
};

export default EmptyCart;
